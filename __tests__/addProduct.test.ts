import { addProduct } from '../lib/shoppingBasket';

describe("addProduct", () => {
    it("Add product to basket", () => {
        expect(addProduct.length).toBeGreaterThan(newProduct.length);
    });
  });